namespace GeoOpenAIWinForms
{
    partial class Form1
    {
        /// <summary>
        /// Limpar os recursos usados.
        /// </summary>
        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
                // No designer, não há componentes a destruir
            }
            base.Dispose(disposing);
        }
    }
}